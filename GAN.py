import tensorflow as tf
class GAN(object):
    def init(self, sess):
        self.sess = sess
        self.imdb = imdb
        self.names_class = names_class
    
    def createGAN(self, config):
        sess.number_classes = number_classes
